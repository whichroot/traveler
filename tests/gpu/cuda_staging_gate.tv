import "cuda_staging_checks.tv";
extern "C" fn strtoull(text: *u8, end: **u8, base: i32) -> u64;
fn main(argc: i32, argv: **u8) -> i32 {
    if argc != 3 { return 1; } staging_pipeline(argv[1], strtoull(argv[2], null, 10)); return 0;
}
