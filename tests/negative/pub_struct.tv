// Negative test: pub struct should parse correctly (not crash)
field F = Field<251>;

pub struct Point {
    x: F,
    y: F,
}

fn main() {
    let p: Point = Point { x: 10, y: 20 };
    print(p.x);
    print(p.y);
}
