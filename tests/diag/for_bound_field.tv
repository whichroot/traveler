// #54: a field element has no iteration order — a field-typed for-loop bound
// refuses (the membrane rule: ordering lives outside the field).
type F = Field<251>;

fn main() -> i32 {
    let x: F = 10;
    var c: i64 = 0;
    for i in 0..x { c = c + 1; }
    print(c);
    return 0;
}
