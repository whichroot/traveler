// atomic_order_bad.tv — an atomic order must be a bare order identifier,
// and atomic_load refuses store-side orders.
fn main() -> i32 {
    let p: *i64 = alloc(1);
    atomic_store(p, 1, 3);
    let v: i64 = atomic_load(p, release);
    return 0;
}
