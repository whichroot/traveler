// Negative: `type` in Phase 1 is field-alias unification only (RHS must be a
// field type — Field/BinField/ExtField, deriving the kind from the RHS). A
// general Rust-style alias to a primitive/struct is NEW semantics (not an alias
// of existing syntax) and is a separately-gated future feature, so it is refused
// here with a clean diagnostic. plan-syntax-modernization Phase 1.
type Byte = u8;

fn main() -> i32 {
    return 0;
}
